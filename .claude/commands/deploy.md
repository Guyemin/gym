Run the following bash commands in sequence to deploy the latest changes to Vercel:

1. `git add .`
2. `git commit -m "$ARGUMENTS"` — if no argument was given, use "update" as the commit message
3. `git push`

After pushing, tell the user that Vercel is deploying and it will be live in about 1 minute.
